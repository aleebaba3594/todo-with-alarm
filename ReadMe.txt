1) install modules by
npx create-react-app myapp

2) attach bootstrap cdn link (only css) in index.html(public folder)

3) run these commands to install external libraries
   npm i shaka-player-react     //shaka player for media....audio and video etc
   npm i react-icons            // react icons